services:
  - type: worker
    name: mlops-orchestrator
    env: docker
    plan: free
    autoDeploy: true
    region: oregon
    buildCommand: ""
    startCommand: "python main.py --stage all --config config.yaml"
    autoRestart: false
  
